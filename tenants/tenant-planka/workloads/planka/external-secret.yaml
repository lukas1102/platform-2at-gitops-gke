apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: secret-dburl
  namespace: tenant-planka
spec:
  data:
  - remoteRef:
      key: tenant-planka-cluster-db-username
    secretKey: username
    metadataPolicy: None
    conversionStrategy: Default
    decodingStrategy: None
  - remoteRef:
      key: tenant-planka-cluster-db-password
    secretKey: password
    metadataPolicy: None
    conversionStrategy: Default
    decodingStrategy: None
  - remoteRef:
      key: tenant-planka-cluster-db-dbname
    secretKey: dbname
    metadataPolicy: None
    conversionStrategy: Default
    decodingStrategy: None
  refreshInterval: 1h
  secretStoreRef:
      name: gcpsm-cluster-secret-store
      kind: ClusterSecretStore
  target:
    creationPolicy: Owner
    name: secret-dburl
    template:
      data:
        uri: postgresql://{{ .username }}:{{ .password }}@cluster-rw.tenant-planka.svc.cluster.local:5432/{{ .dbname }}
      engineVersion: v2
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: secret
  namespace: tenant-planka
spec:
  data:
  - remoteRef:
      key: tenant-planka-cluster-db-password
    secretKey: key
    metadataPolicy: None
    conversionStrategy: Default
    decodingStrategy: None
  refreshInterval: 1h
  secretStoreRef:
      name: gcpsm-cluster-secret-store
      kind: ClusterSecretStore
  target:
    creationPolicy: Owner
    name: secret
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: secret-admin
  namespace: tenant-planka
spec:
  data:
  - remoteRef:
      key: tenant-planka-cluster-db-password
    secretKey: password
    metadataPolicy: None
    conversionStrategy: Default	
    decodingStrategy: None
  - remoteRef:
      key: tenant-planka-cluster-db-username
    secretKey: username
    metadataPolicy: None
    conversionStrategy: Default
    decodingStrategy: None
  refreshInterval: 1h
  secretStoreRef:
    name: gcpsm-cluster-secret-store
    kind: ClusterSecretStore
  target:
    creationPolicy: Owner
    name: secret-admin
