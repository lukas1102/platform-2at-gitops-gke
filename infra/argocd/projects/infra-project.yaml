---
# Project for all cluster-wide infrastructure tools
apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: infra
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  description: "Cluster infrastructure: ArgoCD, cert-manager, ESO, ExternalDNS, Crossplane, cloudnative-pg"

  sourceRepos:
    - "https://github.com/lukas1102/platform-2at-gitops-gke.git"
    - "https://charts.jetstack.io"
    - "https://kubernetes-sigs.github.io/external-dns/"
    - "https://charts.external-secrets.io"
    - "https://charts.crossplane.io/stable"
    - "https://argoproj.github.io/argo-helm"
    - "https://gateway.envoyproxy.io"
    - "https://cloudnative-pg.github.io/charts"

  destinations:
    - server: https://kubernetes.default.svc
      namespace: cert-manager
    - server: https://kubernetes.default.svc
      namespace: external-dns
    - server: https://kubernetes.default.svc
      namespace: external-secrets
    - server: https://kubernetes.default.svc
      namespace: crossplane-system
    - server: https://kubernetes.default.svc
      namespace: argocd
    - server: https://kubernetes.default.svc
      namespace: envoy-gateway-system
    - server: https://kubernetes.default.svc
      namespace: cnpg-operator

  clusterResourceWhitelist:
    - group: "*"
      kind: "*"

  namespaceResourceWhitelist:
    - group: "*"
      kind: "*"

  roles:
    - name: infra-admin
      description: Full access to infra project
      policies:
        - p, proj:infra:infra-admin, applications, *, infra/*, allow
      groups:
        - argocd-admins
